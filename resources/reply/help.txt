1. 说key答value，将会添加一条新的回复规则
2. !list，列出当前回复规则
3. !删除(或!del)+序号，删除对应的规则。删除多个用空格隔开
4. !clear，清空列表
5. 触发条件为全匹配，支持正则表达式
6. 当前仅只支持纯文本消息
7. 支持使用()捕获匹配，在回答中加入{}或{[序号]}替换捕获到的匹配（多个捕获将按顺序替换）。
例1：说不许(.*)答不许不许{}
例2：说(.*)茶茶(.*)答{0}llgl{1} 或 说(.*)茶茶(.*)答{}llgl{}